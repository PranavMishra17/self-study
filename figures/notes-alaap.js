/* Notes for the Alaap and TrenTorch page's figures: al:<diagram> for the plan's diagrams, al:arch-<id> for the architecture figures. See figures/notes.js. */
window.FIG_NOTES = Object.assign(window.FIG_NOTES || {}, {
});
